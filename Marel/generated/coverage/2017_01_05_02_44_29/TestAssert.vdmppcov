class TestAssert
operations
	public assertTrue : bool ==> () 
		assertTrue(cond) == return
	pre cond;
	
	public assertFalse : bool ==> ()
		assertFalse(cond) ==	return
	pre not cond;
                
end TestAssert